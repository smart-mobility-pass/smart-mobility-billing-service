# ===========================
# Application
# ===========================
spring.application.name=billing-service
# Localhost by default for IDE, overridden by CONFIG_SERVER_URL env var in Docker
spring.config.import=optional:configserver:http://${CONFIG_SERVER_URL:localhost}:8888
# server.port=${billing-service.uriport:8085}

# ===========================
# Spring Cloud Config Client
# ===========================
# spring.config.import=optional:configserver:http://localhost:8888
# spring.cloud.config.name=billing-service
# spring.cloud.config.fail-fast=false

# ===========================
# DataSource
# ===========================
# spring.datasource.url=${billing-service.datasource.url}
# spring.datasource.username=${billing-service.datasource.username}
# spring.datasource.password=${billing-service.datasource.password}
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===========================
# JPA
# ===========================
# spring.jpa.hibernate.ddl-auto=${billing-service.jpa.hibernate.ddl-auto}
# spring.jpa.show-sql=${billing-service.jpa.showsql}
# spring.jpa.properties.hibernate.format_sql=true

# ===========================
# RabbitMQ
# ===========================
# spring.rabbitmq.host=localhost
# spring.rabbitmq.port=5672
# spring.rabbitmq.username=guest
# spring.rabbitmq.password=guest
# spring.rabbitmq.listener.simple.retry.enabled=true
# spring.rabbitmq.listener.simple.retry.initial-interval=1000
# spring.rabbitmq.listener.simple.retry.max-retries=3
# spring.rabbitmq.listener.simple.retry.multiplier=2.0
# spring.rabbitmq.listener.simple.retry.max-interval=10000

# ===========================
# Eureka Client
# ===========================
# eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
# eureka.instance.prefer-ip-address=true
# eureka.client.register-with-eureka=true
# eureka.client.fetch-registry=true

# ===========================
# Actuator
# ===========================
management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.info.env.enabled=true
management.endpoint.health.show-details=always
# management.tracing.sampling.probability=1.0

# ===========================
# Zipkin
# ===========================
# management.zipkin.tracing.endpoint=http://localhost:9411/api/v2/spans

# ===========================
# Billing Business Config
# ===========================
# billing.daily-cap=50000

# ===========================
# Logging
# ===========================
# logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%X{traceId}/%X{spanId}] %-5level %logger{36} - %msg%n
# logging.level.com.smart.mobility=DEBUG

